import { createSlice } from "@reduxjs/toolkit";

const appSlice=createSlice({
    name:'appSlice',
    initialState:{
        dumpedText:''
    },
    reducers:{
        
    }
})

export default appSlice;